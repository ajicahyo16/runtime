# Lacify project instructions

Follow the Lacify Runtime v1 AI authoring guide. Keep Actor migrations forward-only, never include secret values, validate and plan before proposing apply, and require explicit user approval for Development mutation. Staging and Production changes must use immutable release governance.
